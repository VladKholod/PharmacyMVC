@model IEnumerable<Pharmacy.Web.Core.Models.Orders.OrderViewModel>
@using GridMvc.Html

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Orders</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-success" })
</p>

@Html.Grid(Model).Columns(columns =>
{
    columns.Add(m => m.PharmacyNumber)
        .Titled("Pharmacy name");

    columns.Add(m => m.Type)
        .Titled("Type");

    columns.Add(m => m.Date)
        .Titled("Phone");

    columns.Add(m => m.Id)
        .Titled("")
        .SetWidth(225)
        .Sanitized(false)
        .Encoded(false)
        .@RenderValueAs(m =>
            @<b>
                @Html.ActionLink("Edit", "Edit", new { id = m.Id },
                    new { @class = " btn btn-default" })

                @Html.ActionLink("Details", "Details", new { id = m.Id },
                    new { @class = " btn btn-default" })

                @Ajax.ActionLink("Delete", "Delete", new { id = m.Id },
                    new AjaxOptions() { HttpMethod = "GET" },
                    new { @class = "btn btn-danger" })
            </b>);

}).WithPaging(5)

@section scripts{
    <script>
        $("#order").addClass("active");
    </script>
}